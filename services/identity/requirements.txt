# Kimlik servisinin çalışma zamanı bağımlılıkları.
#
# K11 — bağımlılık İLAN EDİLİR, kopyalanmaz. Bu liste `backend/pyproject.toml`
# içindeki çekirdek bağımlılıklarının ALT KÜMESİDİR: servis `km_core`'un yalnız
# store + security + config parçalarını kullanır, ssh/printer/audio/notify
# yeteneklerini hiç import etmez.
#
# Sürümler `pyproject.toml` ile aynı alt sınırlarda tutulur; ikisi ayrışırsa
# aynı kod iki ortamda farklı davranır.

fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9
pyyaml>=6.0
argon2-cffi>=23.1
structlog>=24.4
aiosqlite>=0.20

# ADR 0025 — dağıtılan sırlar veritabanında düz durmaz; Fernet ile şifrelenir
# (`services/identity/app/vault.py`). Kontrol Merkezi tarafında `cryptography`
# argon2/asyncssh üzerinden dolaylı geliyor; BU KAPTA GELMİYOR ve ilan
# edilmezse `import cryptography.fernet` konteyner açılışında patlar (K11 —
# bağımlılık ilan edilir, varsayılmaz).
cryptography>=43.0
