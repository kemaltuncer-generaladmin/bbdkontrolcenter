# Kantin geçidi varsayılanları.
# Bu kurulumun ortak kantin API adresi; yerel/test ortamı config/local.yaml ile
# kendi adresini verebilir.
base_url: "https://kantin.bbdstore.com.tr"
device_name: "Kontrol Merkezi"
timeout_seconds: 20
