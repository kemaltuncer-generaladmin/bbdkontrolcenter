# Müşteriler

Mağaza müşteri kayıtları, adresler ve sipariş geçmişi.

**Durum:** iskelet. `module.yaml` sözleşmesi sabit, kod yazılmadı
(`enabled: false`). Kodlama başlarken `tools/module-template/` kopyalanır.

Grup: **BBD Store** · İzinler: `store_customers.view`, `store_customers.manage`
