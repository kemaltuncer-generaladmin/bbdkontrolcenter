# İadeler modülü — İSKELET
# Sözleşme burada sabittir; kod henüz yazılmadı, bu yüzden enabled: false.
# Şema: docs/schemas/module.schema.json · Kılavuz: docs/module-guide.md

id: store_refunds
name: "İadeler"
description: "İade taleplerinin işlenmesi ve para iadesi takibi."
version: 0.1.0
sdk: ">=0.1,<1.0"
enabled: false

entrypoint: backend.module:register

depends: []
provides: []
consumes: []

events:
  publishes: []
  subscribes: []

http:
  prefix: /api/store_refunds
  router: backend.api.routes:router
  tags: ["store_refunds"]
  requires: [store_refunds.view]

ui:
  entry: ui/panel/index.js
  locales: ui/locales
  nav:
    title: "İadeler"
    icon: rotate
    group: "BBD Store"
    order: 140
    requires: [store_refunds.view]

# Katalog ve rol matrisi: docs/permissions.md
# GELİŞTİRME KURALI: ekranlar şimdilik tüm rollere açık. Daraltma, o modülün
# kodlaması başlarken yapılacak.
permissions:
  - key: store_refunds.view
    description: "İadeler ekranını görüntüleme."
    default_roles: [admin, bbd_staff, bld_staff, org_staff, accountant]
  - key: store_refunds.manage
    description: "İadeler kayıtlarını düzenleme."
    default_roles: [admin, bbd_staff, bld_staff, org_staff, accountant]

config:
  defaults: config/default.yaml
  schema: config/schema.json

migrations: backend/migrations

tasks: []
