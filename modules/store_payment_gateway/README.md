# Sanal POS

Sanal POS yapılandırması ve ödeme işlemlerinin izlenmesi.

**Durum:** iskelet. `module.yaml` sözleşmesi sabit, kod yazılmadı
(`enabled: false`). Kodlama başlarken `tools/module-template/` kopyalanır.

Grup: **BBD Store** · İzinler: `store_payment_gateway.view`, `store_payment_gateway.manage`
