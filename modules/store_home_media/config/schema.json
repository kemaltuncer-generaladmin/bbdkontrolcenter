{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "store_home_media ayarları",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "channel": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "description": "Bagisto satış kanalı kodu. Her istekte gönderilir."
    },
    "locale": {
      "type": "string",
      "minLength": 2,
      "maxLength": 8,
      "description": "Bagisto dil kodu. Her istekte gönderilir."
    },
    "recommended_slider": {
      "type": "string",
      "pattern": "^[0-9]{1,5}x[0-9]{1,5}$",
      "description": "Ana ekran kayan görseli için önerilen ölçü, ör. 1920x640."
    },
    "sharp_ratio": {
      "type": "number",
      "minimum": 0.5,
      "maximum": 1,
      "description": "Yüklenen görsel önerilenin bu oranından küçükse bulanık sayılır."
    },
    "aspect_tolerance": {
      "type": "integer",
      "minimum": 0,
      "maximum": 50,
      "description": "En/boy oranında hoş görülen sapma yüzdesi. Üstü kırpılma uyarısı verir."
    },
    "max_image_bytes": {
      "type": "integer",
      "minimum": 50000,
      "maximum": 20000000,
      "description": "Gövdede taşınabilecek en büyük görsel (bayt)."
    },
    "allowed_image_types": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Kabul edilen MIME türleri. Liste dışı tür yüklenmez."
    },
    "link_search_limit": {
      "type": "integer",
      "minimum": 5,
      "maximum": 100,
      "description": "Hedef seçicideki ürün aramasında gösterilecek en çok kayıt."
    }
  }
}
